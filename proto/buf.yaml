# Module of plugin options to use in your .proto files.
version: v1
name: buf.build/complex64/protoc-gen-gorm
lint:
  use:
    - DEFAULT
  except:
    - ENUM_VALUE_PREFIX
    - ENUM_ZERO_VALUE_SUFFIX
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
