# Proto files which include `gorm/v2/options.proto`
# compile to Go code that imports "github.com/complex64/protoc-gen-gorm/gormpb";
# Generate the package:
version: v1
plugins:
  - name: go
    out: .
    opt: paths=source_relative
